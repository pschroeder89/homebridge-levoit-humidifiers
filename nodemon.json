{
  "watch": [
    "src"
  ],
  "ext": "ts",
  "ignore": [],
  "exec": "rm -rf ~/.homebridge/accessories/cachedAccessories && tsc && DEBUG=* homebridge -I",
  "signal": "SIGTERM",
  "env": {
    "NODE_OPTIONS": "--trace-warnings"
  }
}