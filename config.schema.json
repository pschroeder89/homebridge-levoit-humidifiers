{
  "pluginAlias": "LevoitAirPurifiers",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Levoit Air Purifiers"
      },
      "email": {
        "title": "Email",
        "type": "string",
        "required": true,
        "description": "VeSync's account email."
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "description": "VeSync's account password"
      }
    }
  }
}
